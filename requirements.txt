# Azure Functions runtime
azure-functions==1.*

# Azure SDK – Table Storage
azure-data-tables==12.*

# Azure SDK – identity (Managed Identity / DefaultAzureCredential)
azure-identity==1.*

# Azure SDK – Key Vault secrets (SMTP password retrieval)
azure-keyvault-secrets==4.*

# HTTP client for Microsoft Graph API calls
requests==2.*
